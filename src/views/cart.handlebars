<header>
    <h1>Carrito</h1>
</header>

<main>
    <section>
        <ol>
            {{#each data.docs}}
            <li>{{this.name}}</li>
            <ul>
                {{#each this.products}}
                <li>{{this.name}}, {{this.price}}</li>
                {{/each}}
            </ul>
            {{/each}}
        </ol>
    </section>
</main>